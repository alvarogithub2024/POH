/*import { TestBed } from '@angular/core/testing';
import { PreferenciasService } from 'src/app/preferences/preferencias.service';

describe('PreferenciasService', () => {
  let service: PreferenciasService;

  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PreferenciasService]
    });
    service = TestBed.inject(PreferenciasService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
*/
